{
    "name": "hyf-databases-week2",
    "version": "1.0.0",
    "description": "database week2",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "prestart": "npm run build",
        "start": "node dist/index.js",
        "build": "babel src/ -d dist/ --source-maps",
        "watch": "babel -w src/ -d dist/ --source-maps"
    },
    "keywords": [
        "mysql",
        "nodejs"
    ],
    "author": "Richard Paredes <chard.paredes@gmail.com>",
    "license": "ISC",
    "devDependencies": {
        "babel-cli": "6.26.0",
        "babel-preset-env": "1.6.1"
    },
    "dependencies": {
        "mysql": "^2.16.0"
    }
}
